version: '3.8'

services:
  agent-server:
    build:
      context: .
      dockerfile: Dockerfile.server
    ports:
      - "8080:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - agent-network

networks:
  agent-network:
    driver: bridge
